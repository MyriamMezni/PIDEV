{% extends "Back.html.twig" %}
{% block content %}
    {{ form_start(form) }}

    <div class="card-body">
        <div class="form-group">
            {{ form_label(form.numTable) }}
            {{ form_widget(form.numTable,{'attr':{'class':'form-control'} }) }}
        </div>
    </div>
    <div class="card-footer">
        {{ form_widget(form.Valider,{'attr':{'class':'btn btn-primary'} }) }}
    </div>
    {{ form_end(form) }}
{% endblock %}